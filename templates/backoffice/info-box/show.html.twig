{% extends 'base.html.twig' %}

{% block title %}InfoBox{% endblock %}

{% block body %}
    <h1>InfoBox</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info_box.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ info_box.title }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ info_box.body }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ info_box.active ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>BoxType</th>
                <td>{{ info_box.boxType }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backoffice_info_box_index') }}">back to list</a>

    <a href="{{ path('backoffice_info_box_edit', {'id': info_box.id}) }}">edit</a>

    {{ include('backoffice_info_box/_delete_form.html.twig') }}
{% endblock %}
