<form method="post" action="{{ path('backoffice_user_delete', {'id': backoffice_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ backoffice_user.id) }}">
    <button class="btn">Delete</button>
</form>
