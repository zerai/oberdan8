{
    "require-dev": {
        "php": ">=7.4 || 8.0",
        "deployer/deployer": "*",
        "deployer/recipes": "^6.1"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
