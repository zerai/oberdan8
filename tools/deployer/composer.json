{
    "require-dev": {
        "deployer/deployer": "^6.8",
        "deployer/recipes": "^6.1"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
