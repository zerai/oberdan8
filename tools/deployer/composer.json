{
    "require-dev": {
        "php": ">=7.4 || 8.0",
        "deployer/deployer": "^7.4"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
