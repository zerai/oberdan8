{
    "require-dev": {
        "deployer/deployer": "^6.8",
        "deployer/recipes": "^6.2"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
